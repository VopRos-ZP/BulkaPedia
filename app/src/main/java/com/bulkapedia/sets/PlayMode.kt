package com.bulkapedia.sets

import com.bulkapedia.R

enum class PlayMode (val str: Int) {
    SQUAD(R.string.squad_mode),
    BATTLE_ROYAL(R.string.battle_royale_mode),
    ALL(R.string.all_modes);
}